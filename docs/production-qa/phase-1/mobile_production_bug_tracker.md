# KOVARI — Mobile Production Bug Tracker (Phase 1)

This tracker maps confirmed production bugs discovered during the manual QA validation phase. All items must be forensic-investigated and given separate fix/hotfix cards.

| Bug ID | Module | Platform | Severity | Expected Behavior | Actual Behavior | Root Cause Analysis | Action |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **BUG-A1** | Auth | Mobile | P3 | User gets clear formatted error on invalid credentials; local client-side validation detects invalid email format. | Displays generic "Exception: email login failed" string; allows malformed email request submission. | `AuthService.loginWithEmail` catches failure and throws generic `Exception` instead of parsing API response error field or passing exception down, causing `ApiErrorHandler` to fall back to generic string formatting. | Create separate engineering task to enforce standard email regex validation on client and pass API error body messages correctly in `AuthService`. |
| **BUG-A9** | Auth | Mobile | P3 | Incorrect OTP displays a specific user-friendly error (e.g. "Incorrect verification code. Please try again."). | Displays generic "Exception: OTP verification failed". Resend and correct OTP flow work correctly. | Same root cause as BUG-A1: `AuthService.verifyOtp` (or equivalent) wraps the caught error in a generic `Exception` instead of re-throwing the `DioException` or parsing the API error body. | Bundle fix with BUG-A1 in a single error-handling improvement task across all `AuthService` methods. |
| **BUG-A7** | Auth | Mobile | **P1** | Reset link in email opens the app directly to the `/reset-password` screen, carrying the token. | **Production QA (2026-08-30):** A–D, F **PASS** on physical device (cold + background deep link, full reset lifecycle). E skipped. | Host-based `kovari://reset-password` dropped by path-only handler; warm links lost on router recreation + resume race. Fixed in Rounds 1–3. | **VERIFIED PASS — QA SIGN-OFF COMPLETE.** See `bug-a7-fix-validation.md`, `bug-a7-forensic-report.md`. |
| **BUG-A10** | Auth | Backend + Mobile | **P2** | After account deletion, re-login with the same credentials must fail with a clear "Account not found / deleted" error. | **Production QA (2026-08-30):** A–D **PASS** on physical device after Round 2 (`31d65bcb`). Re-login blocked with deleted-account message; no session; no Onboarding. | Login omitted deleted guard; post-delete email null breaks lookup; Google sync could reactivate. Fixed in Rounds 1–2. | **VERIFIED PASS — QA SIGN-OFF COMPLETE.** See `bug-a10-fix-validation.md`, `bug-a10-forensic-report.md`. |
| **BUG-E5** | Explore | Mobile + Backend | **P2** | Group cards in the Explore Groups feed render completely and instantly with name, cover image, destination, description, member count, trip dates, creator details, tags, languages, and lifestyle policies. | Round 1: metadata incomplete + Groups tab no first fetch. Round 1 QA A FAIL: infinite loading (solo/group search race). | **Confirmed:** transformer/fallback gaps; shared `hasSearched`; async `performSearch` wrote solo results into `groupMatches` after tab switch. | **VERIFIED PASS — QA SIGN-OFF COMPLETE** (2026-08-30). See `bug-e5-fix-validation.md`. |
| **BUG-G2** | Groups | Mobile | Medium | Approving a join request removes it from the pending UI list immediately and adds the user to the active member list UI. Join/leave/reject flows update local state instantly. | Request stayed in the UI pending list after approval. Accepted member was added in database but not visible in the UI's active member list. | **Root cause (forensic):** Dead provider invalidation targets, missing entity-store force-subscription, stale `getJoinRequests` disk cache, and uncleared `localCacheProvider` keys after mutations. **Fix verified 2026-08-29:** Tests A–F PASS (mutation-side sync). | **VERIFIED PASS (mutation sync).** No further action on accept/reject/remove/leave/join button flows. |
| **BUG-G2b** | Groups + Notifications | Mobile + Backend | High | When another user submits a join request, the group admin's mobile device receives a push notification and the Join Requests sheet / pending badge updates dynamically without manual refresh. | **Test G delivery (2026-08-29):** S1–S6 **PASS**. S2 verified post `40e4884a`. Tap routing closed via **BUG-G2b-TAP** (`0a7ca12b`). | RC-4 + bridge + S2 socket local notification fix. | **VERIFIED PASS (Test G delivery).** |
| **BUG-G2b-TAP** | Groups + Notifications | Mobile | Medium | Tapping a `GROUP_JOIN_REQUEST_RECEIVED` notification opens Group Settings and the Join Requests sheet automatically. | **Production QA (2026-08-29):** Scenarios 1–7 **PASS** on physical device (background, cold-start, foreground local tap, normal group notif, authorization, G2 + chat regression). Fix `0a7ca12b`. | Entity-type-only router branch ignored `type` field. | **VERIFIED PASS — QA SIGN-OFF COMPLETE.** |
| **BUG-G3** | Groups | Mobile | High | Group itinerary additions/updates made on other clients (like web) sync and render on the mobile group itinerary screen (dynamically or on reload/refresh). | **Production QA (2026-08-29):** A–F, H, I **PASS** on physical device. Refresh/tab-focus cross-platform sync verified. Realtime (G) N/A — no socket channel. | `ItineraryStore.subscribe()` skipped refetch when populated; refresh paths omitted `force: true`. | **VERIFIED PASS — QA SIGN-OFF COMPLETE.** |
| **BUG-C1a** | Chat | Mobile | Medium | Switching between multiple accounts on a single mobile device isolates each user's session and inbox data completely. | Conversations from the previously logged-in account leak into the newly logged-in account's inbox UI. | PENDING FORENSIC INVESTIGATION (State/cache cleanup failure in local stores upon logout / session switch). | Create engineering task to ensure complete store purging and navigation state reset during logout/re-authentication. |
| **BUG-C1b** | Chat | Mobile | Low | Tapping a chat push notification opens the conversation and renders incoming messages instantly. | Correct chat opens but latest message delayed until wait/scroll/refresh. | **Confirmed:** concurrent `_hydrate()` race; delta-only sync with stale metadata + empty cache; partner-id skip before `resolvedUuid`; premature empty UI. | **VERIFIED PASS — QA SIGN-OFF COMPLETE** (2026-08-30). See `bug-c1b-fix-validation.md`, `bug-c1b-forensic-report.md`. |
| **BUG-C4a** | Chat | Mobile | High | Composing and sending messages while offline queues them locally and delivers them automatically upon network reconnection. | Outgoing messages composed while offline are silently discarded and never auto-retried or delivered upon reconnection. | **Reconciled:** Original symptom addressed by BUG-C4b outbox/replay (`MutationJournal`, reconnect replay, stuck-`sending` reset, backend `client_id` idempotency). C4b Test B production QA **PASS**. | **RECONCILED — COVERED BY BUG-C4b.** No standalone implementation. See `bug-c4a-reconciliation.md`. |
| **BUG-C4b** | Chat | Mobile + Backend | High | Realtime message sending and receiving operates reliably and consistently when both clients are online. Replaying a lost-ACK message must not create duplicate server records. 15s timeout shows Failed indicator + retry. Group seen ticks persist across reload/reconnect. | Messaging exhibits non-deterministic delivery failures intermittently even while both users have active connections. Re-sent messages with lost ACKs could produce duplicate server-side rows. 15s timeout does not show Failed indicator (Test D). Group seen ticks revert to grey on reload (Test E). | **Test D:** `failed.statePriority=0` was blocked by the monotonic guard in `MessageStore.updateDeliveryStatus` — `failed` could never be applied to any in-flight message. Fixed: guard now allows `failed` from any non-`seen` state. **Test E:** Group seen state stored only in Redis with 1h TTL; after expiry the API returns `delivered`, and mobile hydration overwrites the local `seen` state. Fixed: Redis TTL extended to 14 days; mobile delta sync now applies `_maxDeliveryStatus` to prevent downgrade of locally-known `seen` state. BUG-C4b core idempotency (Test F): unchanged and confirmed PASS. Web client `useGroupChat` hook updated to map `deliveryStatus` from the REST API to fix seen ticks reload issue. | **VERIFIED PASS — QA SIGN-OFF COMPLETE.** Requires no further action. |
| **BUG-N1a** | Notifications | Mobile | High | Push notifications deliver successfully to the system tray while the application is in the background (exactly one tray entry; foreground Home multi-message still works). | **Round 1 QA FAIL:** A — later foreground trays missing; B/D — **double** tray notifications. | **R1:** background handler no-op + presence/batching. **R2:** handler duplicated OS tray display; 15s per-chat dedupe; socket disconnect on `inactive`. | **FIX IMPLEMENTED — HUMAN QA REQUIRED** (Round 2). See `bug-n1a-forensic-report.md`, `bug-n1a-fix-validation.md`. |
| **BUG-N1b** | Notifications | Mobile | Low | When the application is open in the foreground, incoming messages display an in-app banner/notification alert. | Foreground system notification appears, but no custom in-app Home banner or alert is displayed inside the app UI. | PENDING FORENSIC INVESTIGATION (Foreground notification listener does not trigger in-app alert overlay builder). | Create engineering task to bind foreground push/stream events to an in-app banner alert component. |
| **BUG-S1a** | Safety | Mobile | Medium | Block, Unblock, and Report action buttons respond immediately to a single tap. | Unblock and Report buttons are unresponsive, requiring multiple touches to register. | PENDING FORENSIC INVESTIGATION (Gesture detector conflict or duplicate button overlay blocking pointer events). | Create engineering task to audit hit-test areas and gesture detectors on block/report sheets and menus. |
| **BUG-R3** | Regression | Mobile | Medium | Opening the app from a closed state hydates the inbox preview list with correct snippet messages and unread badge counts. | Cold starting the app displays raw/incorrect preview text ("test 21") and completely lacks the unread count badge indicator. | PENDING FORENSIC INVESTIGATION (Mismatched state hydration between cold start database read vs. active foreground socket event payloads). | Create engineering task to ensure inbox preview and unread count badges are fully hydrated from local DB/cache on startup. |
| **BUG-R4** | Regression | Mobile | High | Match requests and group invitations update the requests screen dynamically or reload immediately upon manual refresh. | Requests and invitations do not sync dynamically, and they fail to populate or update even after manual refresh/reload on the Requests screen. Push notifications for requests are also missing. | PENDING FORENSIC INVESTIGATION (Realtime sync event handler or requests stream controller listener fails to update UI model). | Create engineering task to investigate the Requests screen stream listeners and ensure reload triggers purge existing stale lists. |






