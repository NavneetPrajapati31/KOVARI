# KOVARI — Mobile Production Bug Tracker (Phase 1)

This tracker maps confirmed production bugs discovered during the manual QA validation phase. All items must be forensic-investigated and given separate fix/hotfix cards.

| Bug ID | Module | Platform | Severity | Expected Behavior | Actual Behavior | Root Cause Analysis | Action |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **BUG-A1** | Auth | Mobile | P3 | User gets clear formatted error on invalid credentials; local client-side validation detects invalid email format. | Displays generic "Exception: email login failed" string; allows malformed email request submission. | `AuthService.loginWithEmail` catches failure and throws generic `Exception` instead of parsing API response error field or passing exception down, causing `ApiErrorHandler` to fall back to generic string formatting. | Create separate engineering task to enforce standard email regex validation on client and pass API error body messages correctly in `AuthService`. |
| **BUG-A9** | Auth | Mobile | P3 | Incorrect OTP displays a specific user-friendly error (e.g. "Incorrect verification code. Please try again."). | Displays generic "Exception: OTP verification failed". Resend and correct OTP flow work correctly. | Same root cause as BUG-A1: `AuthService.verifyOtp` (or equivalent) wraps the caught error in a generic `Exception` instead of re-throwing the `DioException` or parsing the API error body. | Bundle fix with BUG-A1 in a single error-handling improvement task across all `AuthService` methods. |
| **BUG-A7** | Auth | Mobile | **P1** | Reset link in email opens the app directly to the `/reset-password` screen, carrying the token. | Reset link is invalid / does not open in the app. Password cannot be changed. | **Two-part failure**: (1) The backend generates `kovari://reset-password?token=...` only when `platform=mobile` is sent — confirmed the mobile `AuthService` correctly sends `'platform': 'mobile'`. (2) The `AndroidManifest.xml` registers `kovari://reset-password` as a custom scheme intent-filter, which is correct. However, **there is no deep link listener in the Flutter app** (no `app_links`, `uni_links`, or equivalent package found in `pubspec.yaml` or any source file) to catch the incoming URI and route it to GoRouter's `/reset-password` screen. The link opens in the browser (or silently fails) because the Flutter layer has no handler to intercept it and extract the token. | **P1 Engineering task**: Add `app_links` (or `uni_links`) package, wire an `AppLinks().uriLinkStream` listener in the app's root widget, and redirect to `ResetPasswordRouteData` with the extracted token on `kovari://reset-password` URIs. |
| **BUG-A10** | Auth | Backend | **P2** | After account deletion, re-login with the same credentials must fail with a clear "Account not found / deleted" error. | Login succeeds with deleted credentials and redirects to Onboarding, meaning the account is not truly locked out post-deletion. | The `/api/auth/login` `handleStandardLogin` path (lines 119–123 in `login/route.ts`) selects only `banned, ban_reason, ban_expires_at` — it **does not select or check `isDeleted`**. The delete-account flow correctly sets `isDeleted: true` and nullifies `password_hash`, so `bcrypt.compare` against a null hash should return false — however the **user row is matched first via email** (line 122: `.ilike("email", email)`). The delete-account route also nullifies `email` on the user row (line 130: `email: null`), so the email lookup should fail. The re-login succeeding suggests either the email nullification is not completing (DB trigger or race condition) or the matched row still has the email. Either way the `isDeleted` guard must be added to the login query as a defence-in-depth check. | **P2 Engineering task**: Add `.eq("isDeleted", false)` filter to the Supabase login query in `handleStandardLogin` (and the web path) so deleted accounts are unconditionally blocked at login, regardless of email/password_hash state. |
| **BUG-E5** | Explore | Mobile + Backend | **P2** | Group cards in the Explore Groups feed render completely and instantly with name, cover image, destination, description, member count, trip dates, creator details, tags, languages, and lifestyle policies. | Group cards not rendering correctly or completely. Results not showing instantly. | **Two-layer field mismatch**: (1) `groupTransformer.ts` (backend) outputs `image` and `avatar` for the cover image field, but `GroupModel.fromJson` (mobile, `group.dart`) reads `coverImage` / `cover_image`. The transformer never outputs `coverImage`, so `group.coverImage` is always `null` → cover image never loads. (2) The transformer also omits `description`, `tags`, `languages`, `smokingPolicy`, `drinkingPolicy`, and `dateRange` (date_range) from its output. `GroupMatchCard` renders all of these fields conditionally — when all are null the card shows skeletal/empty sections. (3) For instant rendering: `matchGroups` response goes through a Redis SWR cache (`v1-stable-groups-v10`). On first load there is no cache, so the path hits the DB fallback synchronously — no disk/memory pre-seed like Solo mode's hydration engine — causing visible latency. | **P2 Engineering task**: (a) Fix `groupTransformer.ts` to output `coverImage`, `description`, `tags`, `languages`, `smokingPolicy`, `drinkingPolicy`, and `dateRange` (mapped from DB columns `cover_image`, `description`, `tags`, `languages`, `smoking_policy`, `drinking_policy`, `start_date`/`end_date`). (b) Fix `GroupModel.fromJson` to also accept `image`/`avatar` as fallback cover image keys. (c) Consider pre-seeding `ExploreState.groupMatches` from the local seen-user cache equivalent, or implement optimistic skeleton-to-content transition parity with the Solo feed. |
| **BUG-G2** | Groups | Mobile | Medium | Approving a join request removes it from the pending UI list immediately and adds the user to the active member list UI. | Request stays in the UI pending list after approval. Accepted member is added in database but is not visible in the UI's active member list. | PENDING FORENSIC INVESTIGATION (Backend successfully registers members, suggesting UI-only state binding / sync failure). | Create engineering task to investigate UI state update and list hydration binding in membership screens. |
| **BUG-G3** | Groups | Mobile | High | Group itinerary additions/updates made on other clients (like web) sync and render on the mobile group itinerary screen (dynamically or on reload/refresh). | Itinerary items created on web do not show up on mobile itinerary view at all, even after refresh/reload/reopen. | PENDING FORENSIC INVESTIGATION (Suspected cache hydration or live sync socket handler registration failure for group itineraries). | Create engineering task to investigate group itinerary stream listeners and cache refresh mechanisms on mobile. |
| **BUG-C1a** | Chat | Mobile | Medium | Switching between multiple accounts on a single mobile device isolates each user's session and inbox data completely. | Conversations from the previously logged-in account leak into the newly logged-in account's inbox UI. | PENDING FORENSIC INVESTIGATION (State/cache cleanup failure in local stores upon logout / session switch). | Create engineering task to ensure complete store purging and navigation state reset during logout/re-authentication. |
| **BUG-C1b** | Chat | Mobile | Low | Tapping a chat push notification opens the conversation and renders incoming messages instantly. | The correct chat screen opens but incoming messages do not appear instantly; user has to wait, scroll, or refresh. | PENDING FORENSIC INVESTIGATION (Delay in screen view data fetching relative to push notification routing payload). | Create engineering task to investigate query execution order and message loading priority when opening chats from push deep links. |
| **BUG-C4a** | Chat | Mobile | High | Composing and sending messages while offline queues them locally and delivers them automatically upon network reconnection. | Outgoing messages composed while offline are silently discarded and never auto-retried or delivered upon reconnection. | PENDING FORENSIC INVESTIGATION (Lack of offline message outbox queue persistence or failed retry loop on socket reconnection). | Create engineering task to implement local outbox persistence and automatic retry logic upon socket connection recovery. |
| **BUG-C4b** | Chat | Mobile | High | Realtime message sending and receiving operates reliably and consistently when both clients are online. | Messaging exhibits non-deterministic delivery failures intermittently even while both users have active connections. | PENDING FORENSIC INVESTIGATION (Intermittent socket connectivity drops, packet loss, or server-side messaging queue issues). | Create engineering task to monitor and profile socket lifecycle events and message acknowledgement receipts. |
| **BUG-N1a** | Notifications | Mobile | High | Push notifications deliver successfully to the system tray while the application is in the background. | Notifications do not deliver/appear while backgrounded; they trigger/appear only after the app is manually brought to the foreground. | PENDING FORENSIC INVESTIGATION (Suspected background socket/FCM handling priority or battery saver constraints blocking background wake). | Create engineering task to audit background FCM message handling lifecycle and ensure high priority flags are configured. |
| **BUG-N1b** | Notifications | Mobile | Low | When the application is open in the foreground, incoming messages display an in-app banner/notification alert. | Foreground system notification appears, but no custom in-app Home banner or alert is displayed inside the app UI. | PENDING FORENSIC INVESTIGATION (Foreground notification listener does not trigger in-app alert overlay builder). | Create engineering task to bind foreground push/stream events to an in-app banner alert component. |
| **BUG-S1a** | Safety | Mobile | Medium | Block, Unblock, and Report action buttons respond immediately to a single tap. | Unblock and Report buttons are unresponsive, requiring multiple touches to register. | PENDING FORENSIC INVESTIGATION (Gesture detector conflict or duplicate button overlay blocking pointer events). | Create engineering task to audit hit-test areas and gesture detectors on block/report sheets and menus. |
| **BUG-R3** | Regression | Mobile | Medium | Opening the app from a closed state hydates the inbox preview list with correct snippet messages and unread badge counts. | Cold starting the app displays raw/incorrect preview text ("test 21") and completely lacks the unread count badge indicator. | PENDING FORENSIC INVESTIGATION (Mismatched state hydration between cold start database read vs. active foreground socket event payloads). | Create engineering task to ensure inbox preview and unread count badges are fully hydrated from local DB/cache on startup. |
| **BUG-R4** | Regression | Mobile | High | Match requests and group invitations update the requests screen dynamically or reload immediately upon manual refresh. | Requests and invitations do not sync dynamically, and they fail to populate or update even after manual refresh/reload on the Requests screen. Push notifications for requests are also missing. | PENDING FORENSIC INVESTIGATION (Realtime sync event handler or requests stream controller listener fails to update UI model). | Create engineering task to investigate the Requests screen stream listeners and ensure reload triggers purge existing stale lists. |






