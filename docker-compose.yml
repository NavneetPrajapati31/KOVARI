version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: kovari-redis
    ports:
      - "6380:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    restart: unless-stopped

  # Add other services here if needed in the future
  # postgres:
  #   image: postgres:15
  #   container_name: kovari-postgres
  #   environment:
  #     POSTGRES_DB: kovari
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: password
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   restart: unless-stopped

volumes:
  redis_data:
  # postgres_data:
